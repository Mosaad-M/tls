[package]
name = "tls"
version = "1.0.0"
description = "Pure-Mojo TLS 1.3 + 1.2 implementation (no OpenSSL dependency)"
license = "MIT"
platforms = ["linux-64", "osx-arm64"]

[mojo]
requires = ">=0.26.2"

[dependencies]
tcp = { git = "Mosaad-M/tcp", version = ">=1.0.0" }
